import firebase from 'firebase'

const state = {
}

const getters = {
}

const mutations = {
}

const actions = {
  makePayment ({commit, getters}) {
    const user = getters.user

    // const billingInfo = {
    //   stripeId: payload.stri
    // }
  }
}

export default {
    state,
    getters,
    mutations,
    actions
}
